import { OrdenContactosPipe } from './orden-contactos.pipe';

describe('OrdenContactosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenContactosPipe();
    expect(pipe).toBeTruthy();
  });
});
